document.getElementById('contact-form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Form submission is currently not available on this static site.');
    // Implement a service like Formspree for form submissions.
});